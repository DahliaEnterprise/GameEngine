#include "camera.h"

camera::camera(QObject *parent) : QObject(parent)
{

}

void camera::start()
{
   //QCamera Setup is infeasable at the moment, use a prerendered open source video to demonstrate networking communication.

}
